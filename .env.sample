# Local dev sample env
# Disable Redis queue if you don't have Redis installed
DISABLE_QUEUE=true

# MongoDB (adjust if using Docker)
MONGO_URI=mongodb://localhost:27017/workflowforge

# JWT secret for local dev (don't use in prod)
JWT_SECRET=dev_secret_not_for_prod

# FRONTEND_URL=http://localhost:5173
